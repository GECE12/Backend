doctype html 
html 
  head 
    title Actions 
  body 
    h1=actions.nombre 
    p=actions.id
    p=actions.updatedAt 
    p=actions.createdAt
    form(action = `/actions/${actions.id}?_method=DELETE` method="POST")
      input(type='submit' value='Borrar') 
